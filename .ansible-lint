---
kinds:
  - playbook: "**/*.{yml,yaml}"

skip_list:
  # Some personal configuration/theme repositories intentionally track latest.
  - git-latest
  # Cache/reload-style tasks will be reviewed separately before handler refactors.
  - no-handler
...
